<odoo>
    <record id="view_expense_type_list" model="ir.ui.view">
        <field name="name">project.expense.type.list</field>
        <field name="model">project.expense.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="limit"/>
                <field name="company_id"/>
            </list>
        </field>
    </record>

    <record id="view_expense_type_form" model="ir.ui.view">
        <field name="name">project.expense.type.form</field>
        <field name="model">project.expense.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="limit"/>
                        <field name="company_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_project_expense_type_list" model="ir.actions.act_window">
        <field name="name">Expense Types</field>
        <field name="res_model">project.expense.type</field>
        <field name="view_mode">list,form</field>
    </record>
<!--
    <menuitem
            id="menu_project_expense_root"
            name="Project Expenses0220"
            parent="project.menu_projects"
            sequence="60"/>

    <menuitem
            id="menu_project_expense_type"
            name="Expense Types"
            parent="menu_project_expense_root"
            action="action_project_expense_type_list"/>

-->
</odoo>