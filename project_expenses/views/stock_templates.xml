<odoo>
    <record id="view_project_expense_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.project.expense</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <sheet position="inside">
                <button name="%(action_project_expenses_kanban)d" type="action" string="Project Expenses" class="oe_highlight"/>
            </sheet>
        </field>
    </record>
</odoo>